
server {
    listen      80;
    server_name _;
    charset     utf-8;
    client_max_body_size 75M;

    location /dist {
        alias /home/docker/app/dist;
    }

    location / {
        include uwsgi_params;
        uwsgi_pass unix:/home/docker/app/uwsgi.sock;        
    }
}

server {
    listen     443;

    ssl        on;
    ssl_certificate         /home/docker/app/ssl/domain.crt;
    ssl_certificate_key     /home/docker/app/ssl/domain.key;

    server_name _;
    charset     utf-8;
    client_max_body_size 75M;

    location /dist {
        alias /home/docker/app/dist;
    }

    location / {
        include uwsgi_params;
        uwsgi_pass unix:/home/docker/app/uwsgi.sock;        
    }
}